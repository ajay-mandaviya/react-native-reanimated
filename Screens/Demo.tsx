import { Text, View } from "react-native";
import { Ripple } from "../components";

const Demo = () => {
  return (
    <View style = {{flex :1}}>
      <Ripple/>
    </View>
  );
};
export default Demo;
